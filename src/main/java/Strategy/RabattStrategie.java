package Strategy;

public interface RabattStrategie {
    double berechneRabatt(double preis);
}
/**
 * Interface mit Methode berechneRabatt(double preis)
 * Jede Rabattstrategie muss diese Methode implementieren sonst funktioniert es nicht :(
 */